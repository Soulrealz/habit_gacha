# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Read this before building on `src/`

`docs/status/OPEN-ITEMS.md` lists what is unverified, what must be confirmed on a real
device, and the next steps. The foundation layer has never been run — do not describe it
as working. That file is short; read it in full.
